!************************* KEEPALIVE *************************
keepalive ${ip_last_octet}_${env}${order}_ka_${appname}
  description "keepalive for ${ip_last_octet}_${env}${order}_${appname}"
  ip address ${ip}
  port ${ka_port}
  type ${ka_type}
  uri ${ka_uri}
  active

